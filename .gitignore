# Build output
/dist/
# The built binary at the repo root (anchored so it does NOT match cmd/xocode/).
/xocode

# Plan documents generated at runtime
/.xocode/

# OS / editor cruft
.DS_Store
*.swp

# Local env
.env
